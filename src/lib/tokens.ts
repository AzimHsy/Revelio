// Design-token values needed outside Tailwind. The content-script overlay
// renders inside the inspected page, where the side panel's @theme CSS does
// not exist. Keep in sync with context/ui-context.md.
export const ACCENT = '#6e56f7'
